SHELLY_IP=192.168.1.1
RELAY_ID=0
LAT=44.42
LON=26.10
CRON_FILE=/var/spool/cron/root
SHELLY_SCRIPT=/data/shelly_control.py

# Use OS timezone (Europe/Bucharest already set on Venus OS)
TIME_MODE=local

# If you prefer a fixed offset instead of OS timezone, set:
# TIME_MODE=offset
# TZ_OFFSET=2
